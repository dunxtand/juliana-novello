<ul class="border-t border-black mx-4 mb-24">
    {% for item in friends_col %}
        <li class="px-2 lg:px-10 border-b border-black flex flex-row justify-center items-center h-24">
            <a href="{{ item.url }}" target="_blank" class="text-lg text-center hover:underline">
                {{ item.name }}
            </a>
        </li>
    {% endfor %}
</ul>
